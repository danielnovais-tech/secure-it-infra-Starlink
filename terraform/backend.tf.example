terraform {
  backend "s3" {
    # Update with your S3 bucket for state storage
    bucket         = "starlink-terraform-state"
    key            = "prod/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-state-lock"
  }
}
